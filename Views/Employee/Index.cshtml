@model IEnumerable<ITI_MVC.Models.Employee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Employee</a>
</p>
@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Name)
            </td>
            <td>
                
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table> *@

<div style="border:3px solid black"  id="EmployeeCard">

</div>

<div class="container">
    <div class="row">
        <div class="col-md-4 mb-3">
            <div class="card h-30 w-50">
        @foreach (var item in Model)
        {
                    <h1 class="align-self-center"><a href="#" onclick="GetEmployeeCard(@item.Id)">@item.Name</a></h1>
            @* <partial name="_EmployeeCardPartial" model="item"></partial> *@
        <a asp-action="Edit" asp-route-Id="@item.Id" class="btn btn-outline-primary btn-sm">Edit </a>
        <a asp-action="Details" asp-route-Id="@item.Id" class="btn btn-outline-info btn-sm">Details </a>
        <hr />
        }
    </div>
    </div>
    </div>
</div>

@section Scripts{
    <script>
        function GetEmployeeCard(empId){
            event.preventDefault();

                  $.ajax(
                      {url: "/Employee/Card/" + empId,
                      success: function(result)
                      {
                          console.log(result);
                            document.getElementById("EmployeeCard").innerHTML = result;
                      }
                      });
        }

    </script>
}