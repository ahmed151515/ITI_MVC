@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    @model EmployeeAndMessageAndEmployeeCountViewModel


    <h1>DateNow:@Model.DateNow</h1>
    <h1>numFoEmployees: @Model.EmployeeCount</h1>

    <h1>Employee data</h1>
    <h2>

        @(
            $"{nameof(Model.Name)}: {Model.Name}\n"
            + $"{nameof(Model.Salary)}: {Model.Salary} \n"
            + $"{nameof(Model.Address)}: {Model.Address} \n"

            )

</h2>
}
